name: Build, Check formating, lint

on:
  push: [ main ]

jobs:
  build-and-format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc g++ make libomp-dev clang-format clang-tidy

      - name: Compile
        run: make

      - name: Check Format
        run: ./scripts/check-format.sh

      - name: Lint
        run: ./scripts/lint.sh
